package com.ssmp.startMenu;

import com.ssmp.startMenu.servers_list.ServersAdapter;

public class ServerPassword {
    ServersAdapter.ViewHolder serversAdapter;

    public ServerPassword(ServersAdapter.ViewHolder serversAdapter)
    {
        this.serversAdapter = serversAdapter;
    }
}
